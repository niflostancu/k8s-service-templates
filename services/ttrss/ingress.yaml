apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ttrss
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - http:
      paths:
      - path: /rss
        pathType: Prefix
        backend:
          service: {name: ttrss, port: {number: 80}}

